# frozen_string_literal: true

RSpec.describe SeqScanner do
  it "has a version number" do
    expect(SeqScanner::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
